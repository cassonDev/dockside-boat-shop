// Local/preview stub only — NOT part of the deployed app. In production,
// Netlify's build step (scripts/generate-config.js) generates this file from
// SUPABASE_URL / SUPABASE_ANON_KEY environment variables; it is .gitignored
// and never committed. This stub just avoids a 404 while previewing the
// design here — leaving values empty makes the app show its normal
// "Supabase is not configured" screen rather than a broken script load.
window.__SUPABASE_CONFIG__ = { url: '', anonKey: '' };
